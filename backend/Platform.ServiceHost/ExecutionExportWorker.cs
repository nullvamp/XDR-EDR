using System.Globalization;
using System.Security.Cryptography;
using System.Text;
using System.Text.Json;
using OpenSecurityPlatform.Foundation;
sealed class ExecutionExportWorker(IExecutionExportRepository jobs, IExecutionTelemetryRepository telemetry, IObjectStorage objects, ILogger<ExecutionExportWorker> log) : BackgroundService { static readonly JsonSerializerOptions Json = new(JsonSerializerDefaults.Web); protected override async Task ExecuteAsync(CancellationToken ct) { while (!ct.IsCancellationRequested) { var job = await jobs.ClaimAsync(ct); if (job is null) { await Task.Delay(250, ct); continue; } try { var page = await telemetry.SearchAsync(job.TenantId, job.Query with { Cursor = null, PageSize = Math.Clamp(job.MaximumRecords, 1, 10000) }, ct); var values = page.Items.Take(job.MaximumRecords).ToArray(); var output = job.Format == "csv" ? Csv(values) : Encoding.UTF8.GetBytes(string.Join('\n', values.Select(x => JsonSerializer.Serialize(x, Json))) + '\n'); var hash = Convert.ToHexString(SHA256.HashData(output)).ToLowerInvariant(); var at = DateTimeOffset.UtcNow; var manifest = JsonSerializer.SerializeToUtf8Bytes(new { schemaVersion = "execution-export-manifest.v1", exportId = job.Id, tenantBinding = job.TenantId, job.Format, recordCount = values.Length, job.Query, job.Fields, sha256 = hash, eventSchemaVersion = "execution-event.v1", job.OutputObjectId, job.MetadataObjectId, completedAt = at, memoryContentsIncluded = false }, Json); var metadata = JsonSerializer.SerializeToUtf8Bytes(new { schemaVersion = "execution-export-metadata.v1", exportId = job.Id, job.Query, job.Fields, job.CreatedAt, job.ExpiresAt, outputSha256 = hash }, Json); await Put(job, job.OutputObjectId, output, job.Format == "csv" ? "text/csv" : "application/x-ndjson", ct); await Put(job, job.ManifestObjectId, manifest, "application/json", ct); await Put(job, job.MetadataObjectId, metadata, "application/json", ct); await jobs.CompleteAsync(job.Id, values.Length, output.Length, hash, at, ct); } catch (Exception e) when (e is not OperationCanceledException) { log.LogError(e, "Execution export failed"); await jobs.FailAsync(job.Id, "EXPORT_FAILED", e.GetType().Name, ct); } } } async Task Put(ExecutionExportJob job, Guid id, byte[] bytes, string media, CancellationToken ct) { await using var stream = new MemoryStream(bytes); await objects.UploadAsync(job.TenantId, id.ToString("D"), stream, media, Convert.ToHexString(SHA256.HashData(bytes)).ToLowerInvariant(), ct); } static byte[] Csv(IEnumerable<ExecutionObservation> values) { static string Cell(string? value) { var text = value ?? ""; if (text.Length > 0 && "=+-@\t\r".Contains(text[0])) text = "'" + text; return '"' + text.Replace("\"", "\"\"") + '"'; } var result = new StringBuilder("schemaVersion,eventId,endpointId,observedAt,eventType,sourceProcess,targetProcess,sourcePid,targetPid,operation,handleType,desiredAccess,accessFlags,threadId,startAddress,memoryBase,memorySize,protectionBefore,protectionAfter,sectionIdentity,provider,nativeEventIdentity,evidenceSha256,quality\n"); foreach (var v in values) result.AppendLine(string.Join(',', Cell(v.SchemaVersion), Cell(v.EventId.ToString()), Cell(v.EndpointId.ToString()), Cell(v.ObservedAt.ToString("O")), Cell(v.Kind.ToString()), Cell(v.SourceProcess?.ProcessEntityId), Cell(v.TargetProcess?.ProcessEntityId), Cell(v.SourceProcess?.ProcessId.ToString(CultureInfo.InvariantCulture)), Cell(v.TargetProcess?.ProcessId.ToString(CultureInfo.InvariantCulture)), Cell(v.Native.NativeOperation), Cell(v.Handle?.HandleType), Cell(v.Handle?.DesiredAccess.ToString(CultureInfo.InvariantCulture)), Cell(string.Join(';', v.Handle?.DesiredAccessFlags ?? [])), Cell(v.Thread?.ThreadId.ToString(CultureInfo.InvariantCulture)), Cell(v.Thread?.StartAddress?.ToString(CultureInfo.InvariantCulture)), Cell(v.Memory?.BaseAddress.ToString(CultureInfo.InvariantCulture)), Cell(v.Memory?.Size.ToString(CultureInfo.InvariantCulture)), Cell(v.Memory?.ProtectionBefore?.ToString(CultureInfo.InvariantCulture)), Cell(v.Memory?.ProtectionAfter?.ToString(CultureInfo.InvariantCulture)), Cell(v.Section?.SectionEntityId), Cell(v.Native.Provider), Cell(v.Native.NativeEventIdentity), Cell(v.EvidenceSha256), Cell(string.Join(';', v.DataQualityFlags)))); return Encoding.UTF8.GetBytes(result.ToString()); } }
