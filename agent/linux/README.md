# Linux Agent Packaging

Sprint Zero uses the shared `Platform.Agent` runtime. Linux packaging must supply a hardened systemd unit, dedicated user, read-only filesystem allowances and signed packages. No telemetry collector is enabled.
