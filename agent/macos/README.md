# macOS Agent Packaging

Sprint Zero uses the shared `Platform.Agent` runtime. macOS packaging must supply a signed/notarized launch daemon package and protected data directory. No Endpoint Security entitlement or telemetry collector is enabled.
