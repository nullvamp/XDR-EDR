# Performance Harness

Sprint Zero performance scenarios measure service startup, health latency, authenticated empty-list throughput, registration bursts, heartbeat throughput, object hash verification and message persistence. CI performs a smoke budget; release qualification runs sustained and burst profiles from the v1 backlog against supported deployment tiers.
